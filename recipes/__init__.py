"""Package marker for the recipes app.

This module is intentionally minimal; it exists so Python treats the
``recipes`` directory as a package. Docstring added to satisfy tooling
that expects a module-level docstring.
"""
